@model IEnumerable<SYP.Models.Portal>

@{
    ViewBag.Title = "List";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="tablerow">
    
    <h2>Haber ve Duyuru Listesi</h2>
    <hr />

    <table class="table table-bordered table-hover table-striped">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Baslik)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Resim)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EklenmeTarihi)
            </th>
            <th style="text-align:center"></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td width="20%">
                    @Html.DisplayFor(modelItem => item.Baslik)
                </td>
                <td width="40%">
                    @Html.DisplayFor(modelItem => item.Resim)
                </td>
                <td width="20%"> 
                    @Html.DisplayFor(modelItem => item.EklenmeTarihi)
                </td>
                <td style="text-align:center">
                    @Html.ActionLink("Detay", "Details", new { id = item.Id }, new { @class = "btn btn-info btn-sm" }) |
                    @Html.ActionLink("Düzenle", "Edit", new { id = item.Id }, new { @class = "btn btn-warning btn-sm" }) |
                    @Html.ActionLink("Sil", "Delete", new { id = item.Id }, new { @class = "btn btn-danger btn-sm" })
                </td>
            </tr>
        }

    </table>

    <p>
        @Html.ActionLink("Yeni Haber Ekle", "Create", null, new { @class = "btn btn-primary" })
    </p>
</div>
